# Docker 部署（容器内真实执行）

说明：本项目已移除“docker 执行器模式”（即后端再去调用宿主机 `docker run` 执行命令）。

推荐方式：使用根目录 `Dockerfile` 部署整个应用，然后使用 `local` 执行器在容器内部真实执行命令。

优点：
- 创空间/平台通常不提供 Docker daemon 权限，这种方式更通用
- 命令在容器内执行，隔离性更强，演示更稳定

构建/运行示例见仓库根目录 README。
